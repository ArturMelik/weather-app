🌦️ Weather App (Node.js + Express + OpenWeather API)

Este proyecto es una aplicación del tiempo desarrollada con Node.js y Express. Permite al usuario consultar el clima de cualquier ciudad utilizando la API de OpenWeather, mostrando información como temperatura, sensación térmica, humedad y descripción del clima. Es un ejercicio práctico para aprender a trabajar con APIs externas, rutas en Express y manejo de variables de entorno.

El proyecto incluye el uso de variables de entorno para proteger la API Key, evitando subirla accidentalmente a GitHub. Para esto se utiliza un archivo .env, que está listado en .gitignore para que no se suba al repositorio. Esta es una práctica recomendada para mantener seguras las credenciales y claves privadas.
